name: lando-backdrop-plugin
services:
  node:
    type: node:18
    build:
      - npm run install
    scanner: false
    ssl: false
    sslExpose: false
tooling:
  node:
    service: node
  npm:
    service: node
